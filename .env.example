# Copy this to .env.local for local development (already the default if omitted)
# and to .env.production before building for deployment, pointing at the
# real deployed Worker URL instead of localhost.
VITE_API_BASE=http://localhost:8787
